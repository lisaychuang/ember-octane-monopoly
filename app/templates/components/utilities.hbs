<div class="space utility
    {{if (eq (get-name @board-position 'name') "Electric Company") "electric-company" "waterworks"}}">

    <div class="container">
        <div class="name">{{get-name @board-position 'name'}}</div>

        <div class="ownership">_</div>
        
        <i class="drawing 
        {{if (eq (get-name @board-position 'name') "Electric Company") "far fa-lightbulb" "fas fa-tint"}}"></i>

        <div class="price">Price {{get-name @board-position 'price'}}</div>
    </div>
</div>