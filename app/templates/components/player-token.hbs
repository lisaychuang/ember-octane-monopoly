<div class="player-tokens">
    {{#each this.visibleTokens as |token|}}
    
    <i class="fas fa-{{token}}"></i>
    {{/each}}
    
    {{!-- <i class="fas fa-dog"></i>
    <i class="fas fa-ship"></i>
    <i class="fas fa-space-shuttle"></i> --}}
</div>